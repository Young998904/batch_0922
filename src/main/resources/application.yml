spring:
  batch:
    job:
      names: ${job.name:withParamJob}
#      ${job.name:<??>}job.name ? ??? : ?? ?????? ??
#      jobBuilderFactory.get("<??>") ? ??

#    --spring.batch.job.names=helloWorldJob ? Application Cofiguration ? ?????? ?? ??
    jdbc:
      initialize-schema: ALWAYS
#      spring batch ?? db
  datasource:
    url: jdbc:mariadb://127.0.0.1:3306/batch?useUnicode=true&characterEncoding=utf8&autoReconnect=true&serverTimezone=Asia/Seoul
    driver-class-name:
    username: lldj
    password: lldj123414
  jpa:
    show-sql: true